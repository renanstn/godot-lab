name: Lint Markdown
on:
  push:
    paths:
      - '**.md'
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install -g markdownlint-cli --disable MD025 MD036
      - run: markdownlint '**/*.md'
